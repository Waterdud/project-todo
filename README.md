# TODO List - XSLT Project

Полнофункциональная система управления задачами, реализованная исключительно с помощью **XSLT** и **XML**.

## 🎯 Особенности проекта

- **100% XSLT** - Вся логика сортировки реализована в XSLT без JavaScript
- **3 типа сортировки**: ID, дата (tähtaeg), предмет (oppeaine)
- **Навигация между страницами**: Home, Add, JSON
- **Минималистичный дизайн**: Только серые и белые тона (#ffffff, #eeeeee, #cccccc)
- **Фильтрация по предметам** - поиск задач по учебным дисциплинам
- **JSON экспорт** - возможность экспорта данных в формате JSON
- **Валидная XML Schema** (XSD) для проверки структуры данных

## 📁 Структура файлов

```
📦 todo-project/
├── 📄 tasks.xml               # Основные данные + Home страница
├── 📄 todo_home.xslt          # Главная страница (базовая сортировка)
├── 📄 todo_sort_id.xml        # XML для сортировки по ID
├── 📄 todo_sort_id.xslt       # XSLT сортировки по ID
├── 📄 todo_sort_date.xml      # XML для сортировки по дате
├── 📄 todo_sort_date.xslt     # XSLT сортировки по дате (tähtaeg)
├── 📄 todo_sort_subject.xml   # XML для сортировки по предмету
├── 📄 todo_sort_subject.xslt  # XSLT сортировки по предмету
├── 📄 todo_add.xml            # XML для формы добавления
├── 📄 todo_add.xslt           # Форма добавления новой задачи
├── 📄 todo_json.xml           # XML для JSON страницы
├── 📄 todo_json.xslt          # JSON экспорт и отображение
├── 📄 todo_filter.xslt        # Фильтрация по предметам
├── 📄 todo.xsd                # XML Schema для валидации
├── 📄 todo.php                # PHP обработчик формы (опционально)
├── 🎨 style.css               # Глобальные стили
└── 📖 README.md               # Документация
```

## 🚀 Как использовать

### 1. Основная навигация
- Откройте `tasks.xml` в браузере для **главной страницы**
- Используйте навигационные кнопки для перемещения между страницами:
  - **🏠 Home** - главная страница со всеми задачами
  - **➕ Add Task** - форма добавления новой задачи
  - **📄 JSON** - просмотр данных в формате JSON

### 2. Сортировка данных (только XSLT!)
Три способа сортировки, реализованных через отдельные XSLT-файлы:

#### По ID (по возрастанию):
- Откройте `todo_sort_id.xml`
- Или нажмите кнопку "🔢 По ID" на главной странице

#### По дате выполнения (tähtaeg):
- Откройте `todo_sort_date.xml`
- Или нажмите кнопку "📅 По дате (Tähtaeg)"
- Задачи сортируются от самых срочных к менее срочным

#### По предмету (oppeaine):
- Откройте `todo_sort_subject.xml`
- Или нажмите кнопку "📚 По предмету"
- Задачи группируются по учебным дисциплинам

### 3. Фильтрация по предметам
- На главной странице в разделе "🔍 Фильтр по предмету" кликните на любой предмет
- Будут показаны только задачи по выбранной дисциплине
- Используйте "✖️ Сбросить" для возврата к полному списку

### 4. JSON экспорт
- Перейдите на страницу JSON (`todo_json.xml`)
- Используйте кнопки:
  - **📋 Copy JSON** - скопировать в буфер обмена
  - **💾 Download** - скачать как файл
  - **✅ Validate** - проверить корректность JSON

## 🎨 Дизайн

Проект использует **минималистичный дизайн** с цветовой палитрой:
- `#ffffff` - белый (фон контента)
- `#eeeeee` - светло-серый (элементы интерфейса)
- `#cccccc` - средне-серый (границы, акценты)
- `#f5f5f5` - очень светло-серый (фон страницы)

### Типографика:
- Шрифт: Segoe UI (системный шрифт Windows)
- Минимальные тени и эффекты
- Адаптивный дизайн для мобильных устройств

## 📊 Структура данных XML

```xml
<tasks>
    <task>
        <id>1</id>
        <kuupaev>2025-01-15</kuupaev>      <!-- Дата создания -->
        <tahtaeg>2025-01-20</tahtaeg>       <!-- Крайний срок -->
        <oppeaine>Matemaatika</oppeaine>    <!-- Учебный предмет -->
        <info>Описание задачи</info>         <!-- Дополнительная информация -->
        <ylesanne>Название задачи</ylesanne> <!-- Название/заголовок -->
    </task>
</tasks>
```

## 🔧 Техническая реализация

### XSLT Сортировка
Все три типа сортировки реализованы с помощью элемента `<xsl:sort>`:

```xsl
<!-- По ID (числовая сортировка) -->
<xsl:sort select="id" data-type="number" order="ascending"/>

<!-- По дате (текстовая сортировка дат в формате YYYY-MM-DD) -->
<xsl:sort select="tahtaeg" order="ascending"/>

<!-- По предмету (алфавитная сортировка) -->
<xsl:sort select="oppeaine" order="ascending"/>
```

### Фильтрация XSLT
Фильтрация по предметам реализована через XPath-предикаты:

```xsl
<!-- Показать только задачи определенного предмета -->
<xsl:apply-templates select="tasks/task[oppeaine = $filterSubject]"/>

<!-- Получить уникальные предметы -->
<xsl:for-each select="tasks/task[not(oppeaine = preceding-sibling::task/oppeaine)]">
```

### JSON генерация
JSON создается путем текстовой генерации в XSLT:

```xsl
<xsl:text>{"id": </xsl:text>
<xsl:value-of select="id"/>
<xsl:text>, "title": "</xsl:text>
<xsl:value-of select="ylesanne"/>
<xsl:text>"}</xsl:text>
```

## 🎯 Соответствие требованиям

✅ **Минимум 3 XSLT-шаблона**: Home, Add, JSON  
✅ **Навигация между страницами**: Кнопки на всех страницах  
✅ **3 функции сортировки**: ID, дата, предмет (только XSLT)  
✅ **Минималистичный дизайн**: Серо-белая палитра  
✅ **Фильтрация по предметам**: Поиск в XSLT  
✅ **JSON формат**: Отдельная страница с трансформацией  
✅ **Валидный XML**: С XSD схемой  
✅ **Самостоятельные XSLT**: Все стили и логика внутри XSLT  

## 🌐 Запуск проекта

1. **Простой способ**: Откройте `tasks.xml` в современном браузере (Chrome, Firefox, Safari)
2. **С веб-сервером**: Разместите файлы на веб-сервере с поддержкой PHP (для обработки форм)
3. **Локально**: Используйте простой HTTP-сервер (например, Python: `python -m http.server`)

## 📝 Примечания

- Все стили встроены в XSLT-файлы для автономности
- Сортировка работает без JavaScript - только XSLT
- Проект полностью соответствует стандартам XML/XSLT 1.0
- Адаптивный дизайн поддерживает мобильные устройства
- Код документирован и следует best practices

## 🤝 Расширение функционала

Для добавления новых функций:

1. **Новая сортировка**: Создайте новый XSLT с другим `<xsl:sort>`
2. **Дополнительные фильтры**: Используйте XPath-предикаты в `select`
3. **Новые поля**: Обновите XSD схему и все XSLT-шаблоны
4. **Стили**: Модифицируйте CSS внутри каждого XSLT

---

**Автор**: TODO Project Team  
**Версия**: 1.0  
**Дата**: Декабрь 2025
